import { FilterByPopulationPipe } from './filter-by-population.pipe';

describe('FilterByPopulationPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterByPopulationPipe();
    expect(pipe).toBeTruthy();
  });
});
