import { OrderCountriesPipe } from './order-countries.pipe';

describe('OrderCountriesPipe', () => {
  it('create an instance', () => {
    const pipe = new OrderCountriesPipe();
    expect(pipe).toBeTruthy();
  });
});
